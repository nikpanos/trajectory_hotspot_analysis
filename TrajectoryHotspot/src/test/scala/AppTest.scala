import java.text.SimpleDateFormat
import java.util.Date

import gr.unipi.ds.msc.{App, Preprocess}
import org.scalatest._

class AppTest extends FlatSpec {
  App.main(null)
  /*val sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss")
  val d = sdf.parse("1988-01-12 14:30:00")
  val timeStart = d.getTime / 1000L
  val d1 = new Date()
  val timeEnd = d1.getTime / 1000L
  println(timeEnd - timeStart)*/
}